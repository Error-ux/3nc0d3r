{
  "name": "neoflow-resolver",
  "version": "1.0.0",
  "description": "Cyberpunk AV1 Encoder Pipeline Control Panel Native App",
  "main": "index.js",
  "scripts": {
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap sync && npx cap run android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}
